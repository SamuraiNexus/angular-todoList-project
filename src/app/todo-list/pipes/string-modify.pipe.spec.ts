import { StringModifyPipe } from './string-modify.pipe';

describe('StringModifyPipe', () => {
  it('create an instance', () => {
    const pipe = new StringModifyPipe();
    expect(pipe).toBeTruthy();
  });
});
